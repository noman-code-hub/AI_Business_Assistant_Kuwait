/**
 * Domain enum/const values live in `../constants`.
 * This module exists to match the documented package layout.
 */
export {
  Role,
  Vertical,
  Locale,
  TenantStatus,
  EntityStatus,
  AppointmentStatus,
  InvoiceStatus,
} from "../constants/index.js";
