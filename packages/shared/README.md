# packages/shared

Single source of truth for:

- Domain types / DTOs
- Enums & constants (roles, verticals, statuses)
- Zod schemas shared by FE + BE
- Error codes

Consumers: `apps/web`, `apps/api`, `packages/ai-service`.
