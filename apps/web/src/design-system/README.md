# Design System Package (Frontend)

Token and theme CSS live in `src/styles/`.

```
design-system/
├── tokens/     → maps CSS vars to TS constants (future)
├── themes/     → theme switch helpers (future)
└── motion/     → Framer Motion variants (future)
```

See `docs/design/01-DESIGN_SYSTEM.md` for the full specification.
Do not implement feature UI until design approval + tooling bootstrap.
